# -*- coding: utf-8 -*-

"""
bandwidth

This file was automatically generated by APIMATIC v2.0 ( https://apimatic.io ).
"""
from bandwidth.utilities.xml_utilities import XmlUtilities


class A2pSettings(object):

    """Implementation of the 'A2pSettings' model.

    TODO: type model description here.

    Attributes:
        message_class (string): TODO: type description here.
        campaign_id (string): TODO: type description here.
        action (Action1Enum): TODO: type description here.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "message_class": 'messageClass',
        "campaign_id": 'campaignId',
        "action": 'action'
    }

    def __init__(self,
                 message_class=None,
                 campaign_id=None,
                 action=None):
        """Constructor for the A2pSettings class"""

        # Initialize members of the class
        self.message_class = message_class
        self.campaign_id = campaign_id
        self.action = action

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if dictionary is None:
            return None

        # Extract variables from the dictionary
        message_class = dictionary.get('messageClass')
        campaign_id = dictionary.get('campaignId')
        action = dictionary.get('action')

        # Return an object of this model
        return cls(message_class,
                   campaign_id,
                   action)

    @classmethod
    def from_element(cls, root):
        """Initialize an instance of this class using an xml.etree.Element.

        Args:
            root (string): The root xml element.

        Returns:
            object: An instance of this class.

        """
        message_class = XmlUtilities.value_from_xml_element(root.find('MessageClass'), str)
        campaign_id = XmlUtilities.value_from_xml_element(root.find('CampaignId'), str)
        action = XmlUtilities.value_from_xml_element(root.find('Action'), str)

        return cls(message_class,
                   campaign_id,
                   action)

    def to_xml_sub_element(self, root):
        """Convert this object to an instance of xml.etree.Element.

        Args:
            root (xml.etree.Element): The parent of this xml element.
        """
        XmlUtilities.add_as_subelement(root, self.message_class, 'MessageClass')
        XmlUtilities.add_as_subelement(root, self.campaign_id, 'CampaignId')
        XmlUtilities.add_as_subelement(root, self.action, 'Action')
